// crie uma tela de home com um botão de logout
import { View, Text, Button } from 'react-native';
import { useAuth } from '../../hooks/authHook';

export function Home() {
    return (
        <View>
            <Text>Home</Text>
        </View>
    )
}